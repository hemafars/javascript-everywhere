الفرق بين node  والمتصح هو ان ال node  يعمل في بيئة خارجية عن المتصفح
npm  هي بعض الحزم التي قام مبرمجون بانشاءها لحل بعض المشكلات التي واجهتهم وارادوا ان عدم تكرارها والمساعدة لباقي المبرمجين وتتميز بسهولة الاستخدام لاحتواها علي طريقة التحميل ويمكن لكل مبرمج انشاء الحزمة الخاصة به ونشرها لباقي المبرمجين .
git هو مدير الاصدارات 
gitgub  مدير الاصدار علي الانترنت 
git add اضافة كل شئ
git commit -m
git push -u
gti log --oneline
git pull سحب مستودع

قمت باصلاح بعض الامور مثل تعديل الfor loop  و if 

When do you use let instead of const?
اذا كانت القيمة ثابتة نستخدم  const  ام اذا كانت النتيجة ستتغير لاحقا نستخدم let

What does typeof [] return, and why is that surprising?
نوع البيانات

What is the difference between === and ==, and why do we only use ===?
== تقارن القيمة فقط  ===  تقارن القيمة ونوع البيانات

When would you use while instead of for?
عندما نكون لا نعلم عدد الكرارات التي سيتم تكرارها